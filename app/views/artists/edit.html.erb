<h1>Edit Artist</h1>

<%= form_for @artist do |f| %>
<%= f.label 'Name' %>
<%= f.text_field :name %><br>

<p>
  <%= f.label 'Bio' %>
  <%= f.text_area :bio %><br>
</p>

<%= f.submit "Update Artist" %>

<% end %>
